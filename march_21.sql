/*syntax for creating table
CREATE TABLE table_name(col1 datatype, col datatype, .....);*/
DDL stmts are related to Structure of the database.

CREATE TABLE gadgets(id int, g_name varchar(20), price bigint);

 SELECT * FROM table_name;
 
 SELECT * FROM gadgets;
 use nov_27;
 SELECT * FROM product;
 DESC product;
 
gadgets Task:
 CREATE 5 tables with 3 columns(Each table 3 columns);
 
 
 
 
 
